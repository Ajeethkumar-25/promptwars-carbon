[build]
  command = "npm run build"
  publish = "dist"

# Required for React Router to handle navigation correctly without returning 404s
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
